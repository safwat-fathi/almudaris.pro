## 2024-05-01 - [Parallelize Backend Data Fetching]
**Learning:** Sequential data fetching in server components creates unnecessary network waterfalls, significantly degrading page load times. The `groupsService` and `teachersService` API endpoints are often fetched together but are entirely independent.
**Action:** Use `Promise.all` to fetch independent data sources concurrently in Next.js Server Components.